name: CI

on:
  push:
    branches:
      - main
      - feature/**
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Sync development dependencies
        run: uv sync --group dev

      - name: Run test suite
        run: uv run -m pytest

  package:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Build distributions
        run: uv build

      - name: Smoke test installed wheel
        env:
          SCRIBA_HOME: ${{ runner.temp }}/scriba-home
        run: |
          uv tool run --from dist/*.whl scriba --help
          uv tool run --from dist/*.whl scriba doctor --preset passthrough --input samples/docs/mini_api.md
          uv tool run --from dist/*.whl scriba run --preset passthrough --input samples/docs/mini_api.md --run-id ci-smoke --output ${{ runner.temp }}/scriba-output
          uv tool run --from dist/*.whl scriba status --run-id ci-smoke

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: scriba-dist
          path: dist/*
